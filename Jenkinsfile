@Library('roboshop-jenkins-library') _  // declaring libray-jenkins configuration
env.appName="ecomm-shipping"
env.appType="maven"// sending a paramter to identify type|sice we dotn want to run compile for node
ci_pipeline() //calling the pipeline-ci-jenkins-prjt-infra